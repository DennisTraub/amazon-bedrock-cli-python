[project]
name = "amazon-bedrock-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "amazon-bedrock>=0.0.5",
]

[project.optional-dependencies]
dev = [
    "amazon-bedrock @ file:///${PWD}/amazon-bedrock-python"
]
